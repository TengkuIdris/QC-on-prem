Use the following prompt exactly as written. Do not try to read local files. Apply the changes to the current React + TypeScript + Vite repo and return a single unified diff patch only. If you need to add files, include them in the same patch. After the patch, output the commit message and PR description as specified. Here is the prompt: (paste the full contents of prompt.md here)